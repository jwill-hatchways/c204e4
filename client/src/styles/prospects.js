import { makeStyles } from "@material-ui/core";

export const useProspectStyles = makeStyles((theme) => ({
    marginEnd: {
        marginEnd: "2rem",
    },
    formFieldWidth: {
        width: "100%",
    }

}));
  